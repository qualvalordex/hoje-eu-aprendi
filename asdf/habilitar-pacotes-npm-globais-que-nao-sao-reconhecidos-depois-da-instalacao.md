> Problema: executar o comando gitmoji depois de instalar o gitmoji-cli

Toda vez que instalar um pacote global no nodejs instalado via asdf, é necessário utilizar o comando
```
$ asdf reshim nodejs
```